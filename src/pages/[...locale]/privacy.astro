---
// /privacy and /zh/privacy — what the site measures and which third parties see
// it. Keep this page in sync with the actual analytics tags in BaseLayout.astro
// and StarlightHead.astro: if a tag is added or removed, this copy changes too.
import '@fontsource-variable/geist';
import '@fontsource-variable/jetbrains-mono';
import '../../styles/tokens.css';
import '../../styles/landing.css';
import '../../styles/blog.css';
import TrustPage from '../../components/TrustPage.astro';
import { localeStaticPaths, toLocale } from '../../i18n';

export function getStaticPaths() { return localeStaticPaths(); }
const locale = toLocale(Astro.params.locale);
---

<TrustPage locale={locale} page="privacy" path="/privacy/" schemaType="WebPage" updated="2026-08-05" />
