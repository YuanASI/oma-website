---
title: Introduction
description: "Understand what open-multi-agent does, how goal-first orchestration works, and when to use it."
---

import { Card, CardGrid } from '@astrojs/starlight/components';

`open-multi-agent` is a TypeScript-native orchestration framework for Node.js backends. It lets specialist agents work as one team: for a non-trivial goal, `runTeam()` can turn the outcome into a task DAG, run independent work in parallel, and synthesize the results.

:::tip[Start with the outcome, not a workflow diagram]
Describe what the team should achieve. OMA can decide how to divide the work at runtime. If the task is simple or the graph is already known, you can choose a lighter or more explicit run mode instead.
:::

## Is OMA a fit?

<CardGrid>
  <Card title="Use OMA when" icon="approve-check">
    - A goal benefits from different specialist roles.
    - Some work can run in parallel.
    - The steps may change with the input.
    - You want orchestration inside an existing Node.js backend.
  </Card>
  <Card title="Keep it simpler when" icon="laptop">
    - One model call already solves the task.
    - Every step and transition must stay fixed.
    - You only need a provider SDK or chat UI.
    - You do not want the cost of an extra planning call.
  </Card>
</CardGrid>

## One run in four steps

1. **Goal** — your application supplies an outcome and a team of agents.
2. **Plan** — for a non-trivial `runTeam()` goal, the coordinator creates tasks, dependencies, and assignments. A simple goal may go directly to one agent.
3. **Execute** — the scheduler unblocks tasks as their dependencies finish and runs independent tasks in parallel.
4. **Result** — `runTeam()` synthesizes the task outputs. Individual agents can also return schema-validated structured data when you configure an `outputSchema`.

The [architecture page](/architecture/) shows the runtime components behind this flow.

## Four core concepts

<CardGrid>
  <Card title="Agent" icon="laptop">
    A role with a model, instructions, and an explicit set of tools.
  </Card>
  <Card title="Team" icon="random">
    The agents available for one goal, with optional shared memory.
  </Card>
  <Card title="Task" icon="setting">
    A unit of work with an assignee and optional dependencies.
  </Card>
  <Card title="Orchestrator" icon="approve-check">
    The API that creates teams, schedules work, and returns run results.
  </Card>
</CardGrid>

## What you can configure

<CardGrid>
  <Card title="Models & tools" icon="setting">
    Mix hosted or local [providers](/reference/providers/), grant least-privilege [tools](/reference/tool-configuration/), and connect [external agents](/reference/external-agents/).
  </Card>
  <Card title="Orchestration" icon="random">
    Choose automatic planning or an explicit graph, then add [consensus](/reference/consensus/), [model routing](/reference/model-routing/), or [plan replay](/reference/plan-replay/) when needed.
  </Card>
  <Card title="Reliable operation" icon="approve-check">
    Trace runs with [observability](/reference/observability/), recover with [checkpoints](/reference/checkpoint/), control [context](/reference/context-management/), and measure quality with [evaluation](/reference/evaluation/).
  </Card>
</CardGrid>

## Choose a starting mode

- Use `runAgent()` for one agent and one focused task.
- Start with `runTeam()` when you know the goal but do not want to maintain the task graph.
- Use `runTasks()` when the graph and assignments must be explicit.

The [Quick Start](/getting-started/quick-start/) gives you a no-key first run in about five minutes. Then [Choose a Run Mode](/getting-started/three-ways-to-run/) explains the tradeoffs in detail.

Still comparing frameworks? See OMA alongside LangGraph, CrewAI, AutoGen, and the OpenAI Agents SDK on the [comparison pages](/compare/).
