---
title: Introduction
description: "What open-multi-agent is and a map of what it can do."
---

import { Card, CardGrid } from '@astrojs/starlight/components';

`open-multi-agent` is a multi-agent orchestration framework for TypeScript backends. Give it a goal; a coordinator agent decomposes it into a task DAG, parallelizes independents, and synthesizes a typed, schema-validated result. Three runtime dependencies, drops into any Node.js backend.

:::tip[Your engineers describe the goal, not the graph.]
Graph-first frameworks make you enumerate every node and edge up front. `open-multi-agent` is goal-first: you describe the outcome and the coordinator builds the task DAG at runtime, so the orchestration adapts to the goal instead of being hand-wired for one.
:::

Choosing between frameworks? See the [comparison pages](/compare/) for OMA vs. LangGraph, CrewAI, AutoGen, and the OpenAI Agents SDK.

## What you can do

<CardGrid>
  <Card title="Configure models & tools" icon="setting">
    [Providers](/reference/providers/) · [Tool configuration](/reference/tool-configuration/) · [External agents](/reference/external-agents/)
  </Card>
  <Card title="Control orchestration" icon="random">
    [Consensus](/reference/consensus/) · [Model routing](/reference/model-routing/) · [Plan preview & replay](/reference/plan-replay/) · [Shared memory](/reference/shared-memory/)
  </Card>
  <Card title="Operate reliably" icon="approve-check">
    [Observability](/reference/observability/) · [Checkpoint & resume](/reference/checkpoint/) · [Context management](/reference/context-management/) · [Evaluation](/reference/evaluation/)
  </Card>
  <Card title="Interfaces" icon="laptop">
    [CLI](/reference/cli/)
  </Card>
</CardGrid>

## How it works

```
┌─────────────────────────────────────────────────────────────────┐
│  OpenMultiAgent (Orchestrator)                                  │
│                                                                 │
│  createTeam()  runTeam()  runTasks()  runAgent()  getStatus()   │
└──────────────────────┬──────────────────────────────────────────┘
                       │
            ┌──────────▼──────────┐
            │  Team               │
            │  - AgentConfig[]    │
            │  - MessageBus       │
            │  - TaskQueue        │
            │  - SharedMemory     │
            └──────────┬──────────┘
                       │
         ┌─────────────┴─────────────┐
         │                           │
┌────────▼──────────┐    ┌───────────▼───────────┐
│  AgentPool        │    │  TaskQueue             │
│  - Semaphore      │    │  - dependency graph    │
│  - runParallel()  │    │  - auto unblock        │
└────────┬──────────┘    │  - cascade failure     │
         │               └───────────────────────┘
┌────────▼──────────┐
│  Agent            │
│  - run()          │    ┌────────────────────────┐
│  - prompt()       │───►│  LLMAdapter            │
│  - stream()       │    │  - 13 built-in         │
└────────┬──────────┘    │    providers           │
         │               │  - OpenAI-compatible   │
         │               │  - AI SDK bridge       │
         │               └────────────────────────┘
┌────────▼──────────┐
│  AgentRunner      │    ┌──────────────────────┐
│  - conversation   │───►│  ToolRegistry        │
│    loop           │    │  - defineTool()      │
│  - tool dispatch  │    │  - 6 built-in tools  │
└───────────────────┘    │  + delegate (opt-in) │
                         └──────────────────────┘
```

The [Quick Start](/getting-started/quick-start/) runs this end to end; [Three Ways to Run](/getting-started/three-ways-to-run/) covers when to reach for `runAgent`, `runTeam`, or `runTasks`.
