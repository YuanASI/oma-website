---
version: '1.7.0'
tag: 'v1.7.0'
date: 2026-06-15
url: 'https://github.com/open-multi-agent/open-multi-agent/releases/tag/v1.7.0'
---

## ⚠️ Breaking: built-in tools are now opt-in / default-deny ([#289](https://github.com/open-multi-agent/open-multi-agent/issues/289) by [@JackChen-me](https://github.com/JackChen-me))

A no-tools agent used to receive every built-in implicitly, including an
unsandboxed `bash`. Tool output flows back to the model, so under prompt
injection that was a remotely triggerable exec + exfiltration path. Built-in
tools (`bash`, `file_*`, `grep`, `glob`, `delegate_to_agent`) now need a
positive grant: with neither `tools` nor `toolPreset` set, an agent resolves to
zero.

Migrating:
- One-line restore of the old allow-all: `defaultToolPreset: 'full'` on `OrchestratorConfig`.
- Or grant per agent via `tools` / `toolPreset`.
- Custom tools (`customTools` / `addTool`) are unaffected: registration is the grant, and `disallowedTools` is still honored.

## Features
- **Consensus / adversarial verification** ([#280](https://github.com/open-multi-agent/open-multi-agent/issues/280) by [@CodingBangboo](https://github.com/CodingBangboo)). `runConsensus()` runs proposers against judges, plus an optional per-task `verify` hook that puts a task's own result through the same judge loop.
- **Deterministic model routing** ([#286](https://github.com/open-multi-agent/open-multi-agent/issues/286) by [@cat0825](https://github.com/cat0825)). Opt-in `modelRouting` sends different orchestration calls (coordinator, synthesis, workers, delegated) to different models by match rules, without mutating your team config. Also fixes routing being bypassed on pooled agents.
- **MiniMax-M3 is the new default** ([#292](https://github.com/open-multi-agent/open-multi-agent/issues/292) by [@octo-patch](https://github.com/octo-patch)). Up to a 1M-token context (512K guaranteed) with image input. M2.7 and `MiniMax-M2.7-highspeed` stay available if you pin them explicitly.

## Docs
- Model routing guide: `docs/model-routing.md`, linked from both READMEs ([#293](https://github.com/open-multi-agent/open-multi-agent/issues/293) by [@JackChen-me](https://github.com/JackChen-me)).
- TencentDB-Agent-Memory integration cookbook under `examples/` ([#295](https://github.com/open-multi-agent/open-multi-agent/issues/295) by [@JackChen-me](https://github.com/JackChen-me)).
- LiteLLM added to the OpenAI-compatible providers table ([#283](https://github.com/open-multi-agent/open-multi-agent/issues/283) by [@RheagalFire](https://github.com/RheagalFire)).

## Install
npm install @open-multi-agent/core@1.7.0

Thanks to [@CodingBangboo](https://github.com/CodingBangboo), [@cat0825](https://github.com/cat0825), [@octo-patch](https://github.com/octo-patch), and [@RheagalFire](https://github.com/RheagalFire). Extra thanks to [@CodingBangboo](https://github.com/CodingBangboo), who landed the consensus primitive ([#280](https://github.com/open-multi-agent/open-multi-agent/issues/280)) and has been a steady reviewer this cycle.
